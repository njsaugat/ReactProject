import React from 'react'

export default function IngredientsList({keys,values}) {
    // console.log(keys)
    // console.log(values)
    // console.log(props)

    return (
       <div>
           <span>{keys }</span>
           <span>{values }</span>
       </div>
    )
}
